{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Act creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('act') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
